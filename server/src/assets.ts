// v2 removed bundled Gradle wrapper payloads.
// This file remains as a placeholder so future server-side template assets
// can live in one predictable module path without breaking imports.
export const bundledAssetsRemoved = true;
